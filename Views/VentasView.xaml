<UserControl x:Class="AppCaravana.Views.VentasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F5F6FA">

    <UserControl.Resources>
        <ResourceDictionary Source="../Resources/Styles.xaml"/>
    </UserControl.Resources>

    <Grid Margin="0">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Background="White"
                Padding="25,20"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="💰 Gestión de Ventas"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,10"/>
                <TextBlock Text="Registra y administra todas las transacciones de venta de caravanas"
                           FontSize="13"
                           Foreground="#7F8C8D"/>
            </StackPanel>
        </Border>

        <!-- BOTONES SUPERIORES -->
        <Border Grid.Row="1"
                Background="White"
                Padding="25,15"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0">
            <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center">

                <Button Content="➕ Nueva Venta"
                        Width="180"
                        Height="45"
                        Margin="0,0,10,0"
                        Click="BtnNuevaVenta_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>

                <Button Content="🔄 Refrescar"
                        Width="150"
                        Height="45"
                        Click="BtnRefrescar_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>

            </StackPanel>
        </Border>

        <!-- TABLA DE VENTAS -->
        <Border Grid.Row="2"
                Margin="25,15"
                Background="White"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="#E0E0E0">
            <DataGrid x:Name="dgVentas"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      FontSize="13"
                      RowHeight="45"
                      ColumnHeaderHeight="50"
                      SelectionMode="Single"
                      ColumnWidth="*"
                      Style="{StaticResource ModernDataGridStyle}">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="{x:Type DataGridColumnHeader}"
                            BasedOn="{StaticResource ModernDataGridColumnHeaderStyle}"/>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Fecha"
                                        Binding="{Binding Fecha, StringFormat=\{0:dd/MM/yyyy\}}"
                                        Width="110"/>

                    <DataGridTextColumn Header="Cliente"
                                        Binding="{Binding Cliente.Nombre}"
                                        Width="130"/>

                    <DataGridTextColumn Header="Cant. Caravanas"
                                        Binding="{Binding VentaCaravanas.Count}"
                                        Width="120"/>

                    <DataGridTemplateColumn Header="Caravanas"
                                            Width="230">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding VentaCaravanas}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#eef2f7" Padding="4" Margin="2" CornerRadius="4">
                                                <TextBlock Text="{Binding Caravana.Serie}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Precio Venta"
                                        Binding="{Binding Importe, StringFormat=C}"
                                        Width="130"/>

                    <!-- Acciones -->
                    <DataGridTemplateColumn Header="Acciones"
                            Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">

                                    <Button Content="✏️ Editar"
                                            Width="85"
                                            Height="32"
                                            Margin="3"
                                            Click="BtnEditar_Click"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            FontSize="12"/>

                                    <Button Content="🗑️ Eliminar"
                                            Width="90"
                                            Height="32"
                                            Margin="3"
                                            Click="BtnEliminar_Click"
                                            Style="{StaticResource DangerButtonStyle}"
                                            FontSize="12"/>
                                </StackPanel>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>
        </Border>

    </Grid>

</UserControl>
