<UserControl x:Class="AppCaravana.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F5F6FA">

    <UserControl.Resources>
        <ResourceDictionary Source="../Resources/Styles.xaml"/>
    </UserControl.Resources>

    <Grid Margin="0">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Background="White"
                Padding="25,20"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="👥 Gestión de Clientes"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,10"/>
                <TextBlock Text="Administra el registro de clientes y sus datos de contacto"
                           FontSize="13"
                           Foreground="#7F8C8D"/>
            </StackPanel>
        </Border>

        <!-- BOTONES SUPERIORES -->
        <Border Grid.Row="1"
                Background="White"
                Padding="25,15"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">

                <Button Content="➕ Nuevo Cliente"
                        Width="180"
                        Height="45"
                        Margin="0,0,10,0"
                        Click="BtnNuevoCliente_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>

                <Button Content="🔄 Refrescar"
                        Width="150"
                        Height="45"
                        Click="BtnRefrescar_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>

            </StackPanel>
        </Border>

        <!-- TABLA -->
        <Border Grid.Row="2"
                Margin="25,15"
                Background="White"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="#E0E0E0">
            <DataGrid x:Name="dgClientes"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      FontSize="13"
                      RowHeight="110"
                      ColumnHeaderHeight="50"
                      SelectionMode="Single"
                      ColumnWidth="*"
                      Style="{StaticResource ModernDataGridStyle}">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="{x:Type DataGridColumnHeader}"
                           BasedOn="{StaticResource ModernDataGridColumnHeaderStyle}"/>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Nombre"
                                        Binding="{Binding Nombre}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Apellido"
                                        Binding="{Binding Apellido}"
                                        Width="120"/>

                    <DataGridTextColumn Header="DNI"
                                        Binding="{Binding DNI}"
                                        Width="110"/>

                    <DataGridTextColumn Header="Teléfono"
                                        Binding="{Binding Telefono}"
                                        Width="130"/>

                    <DataGridTextColumn Header="Email"
                                        Binding="{Binding Email}"
                                        Width="200"/>

                    <DataGridTextColumn Header="Fecha Registro"
                                        Binding="{Binding FechaRegistro, StringFormat=\{0:dd/MM/yyyy\}}"
                                        Width="130"/>

                    <DataGridTemplateColumn Header="Código de Cliente"
                                            Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                    <TextBox Text="{Binding CodigoQR}"
                                             IsReadOnly="True"
                                             Width="120"
                                             Margin="0,0,6,0"
                                             ToolTip="Código público del cliente"
                                             HorizontalContentAlignment="Center"/>
                                    <Button Content="📋"
                                            Width="32"
                                            Height="32"
                                            Click="BtnCopiarCodigo_Click"
                                            ToolTip="Copiar código"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Código QR del cliente -->
                    <DataGridTemplateColumn Header="QR"
                                            Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                    <Image Source="{Binding RutaImagenQR}"
                                           Width="120"
                                           Height="120"
                                           Stretch="Uniform"
                                           Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding CodigoQR}"
                                               TextWrapping="Wrap"
                                               FontSize="11"
                                               Foreground="#7F8C8D"
                                               TextAlignment="Center"
                                               Width="180"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- ACCIONES -->
                    <DataGridTemplateColumn Header="Acciones"
                                            Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">

                                    <Button Content="✏️ Editar"
                                            Width="85"
                                            Height="32"
                                            Margin="3"
                                            Click="BtnEditar_Click"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            FontSize="12"/>

                                    <Button Content="🗑️ Eliminar"
                                            Width="90"
                                            Height="32"
                                            Margin="3"
                                            Click="BtnEliminar_Click"
                                            Style="{StaticResource DangerButtonStyle}"
                                            FontSize="12"/>

                                </StackPanel>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>
        </Border>

    </Grid>

</UserControl>
